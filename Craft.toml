[package]
name = "xml"
version = "0.1.0"
kern = "0.7.5"
publish = false
description = "XML parsing and decoding utilities for Kern"
readme = "README.md"
license = "MIT"

[lib]
root = "src/lib.rn"

[test]
roots = ["tests/smoke.rn"]
